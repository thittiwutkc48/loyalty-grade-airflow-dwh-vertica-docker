

CREATE TABLE CLYMAPPO.sgv_true_customers
(
    grading_account_id varchar(100),
    identifier_no varchar(100),
    id_type varchar(100),
    customer_no varchar(100),
    company_name varchar(200),
    customer_status varchar(200),
    customer_type varchar(100),
    first_name varchar(300),
    last_name varchar(300),
    title varchar(300),
    birth_date date,
    gender varchar(20),
    street varchar(300),
    house_number varchar(300),
    moo varchar(300),
    postal_code varchar(300),
    city varchar(300),
    district varchar(300),
    sub_district varchar(300),
    country varchar(300),
    room_number varchar(300),
    floor varchar(300),
    building_mooban varchar(300),
    soi varchar(300),
    email_address varchar(254),
    golden_id varchar(100),
    phone_number_list long varchar(500000),
    emp_employee_id varchar(50),
    emp_company_name varchar(100),
    emp_position_code varchar(50),
    emp_employee_flag varchar(10),
    operator_name varchar(10),
    create_at timestamp DEFAULT now(),
    update_at timestamp DEFAULT now(),
    transaction_date date,
    partition_date date NOT NULL,
    partition_month varchar(10),
    processed_date date
)
PARTITION BY (sgv_true_customers.partition_date);


CREATE PROJECTION CLYMAPPO.sgv_true_customers_super /*+basename(sgv_true_customers),createtype(P)*/ 
(
 grading_account_id,
 identifier_no,
 id_type,
 customer_no,
 company_name,
 customer_status,
 customer_type,
 first_name,
 last_name,
 title,
 birth_date,
 gender,
 street,
 house_number,
 moo,
 postal_code,
 city,
 district,
 sub_district,
 country,
 room_number,
 floor,
 building_mooban,
 soi,
 email_address,
 golden_id,
 phone_number_list,
 emp_employee_id,
 emp_company_name,
 emp_position_code,
 emp_employee_flag,
 operator_name,
 create_at,
 update_at,
 transaction_date,
 partition_date,
 partition_month,
 processed_date
)
AS
 SELECT sgv_true_customers.grading_account_id,
        sgv_true_customers.identifier_no,
        sgv_true_customers.id_type,
        sgv_true_customers.customer_no,
        sgv_true_customers.company_name,
        sgv_true_customers.customer_status,
        sgv_true_customers.customer_type,
        sgv_true_customers.first_name,
        sgv_true_customers.last_name,
        sgv_true_customers.title,
        sgv_true_customers.birth_date,
        sgv_true_customers.gender,
        sgv_true_customers.street,
        sgv_true_customers.house_number,
        sgv_true_customers.moo,
        sgv_true_customers.postal_code,
        sgv_true_customers.city,
        sgv_true_customers.district,
        sgv_true_customers.sub_district,
        sgv_true_customers.country,
        sgv_true_customers.room_number,
        sgv_true_customers.floor,
        sgv_true_customers.building_mooban,
        sgv_true_customers.soi,
        sgv_true_customers.email_address,
        sgv_true_customers.golden_id,
        sgv_true_customers.phone_number_list,
        sgv_true_customers.emp_employee_id,
        sgv_true_customers.emp_company_name,
        sgv_true_customers.emp_position_code,
        sgv_true_customers.emp_employee_flag,
        sgv_true_customers.operator_name,
        sgv_true_customers.create_at,
        sgv_true_customers.update_at,
        sgv_true_customers.transaction_date,
        sgv_true_customers.partition_date,
        sgv_true_customers.partition_month,
        sgv_true_customers.processed_date
 FROM CLYMAPPO.sgv_true_customers
 ORDER BY sgv_true_customers.grading_account_id,
          sgv_true_customers.identifier_no,
          sgv_true_customers.id_type,
          sgv_true_customers.customer_no,
          sgv_true_customers.company_name,
          sgv_true_customers.customer_status,
          sgv_true_customers.customer_type,
          sgv_true_customers.first_name
UNSEGMENTED ALL NODES;


SELECT MARK_DESIGN_KSAFE(1);
